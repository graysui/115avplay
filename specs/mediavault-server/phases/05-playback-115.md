# P5 · 资源调度、任务恢复与清理

[总计划](../plan.md) · [需求](../spec.md) · [追踪](../traceability.md)

## 范围与依赖

前置：P1、P2；无需等待P4，最小链路使用已入库资源。

对应设计：主设计§8及扫描清理规范。对应需求：FR-STREAM-1～6、FR-OPS-4。本文件定义待实施任务，不是通过记录。

## 任务清单

| ID | 状态 | 优先级 | 任务 |
|---|---|---|---|
| T-501 | [x] | 必须 | 统一新会话/已有租约两种可播谓词；所有查询带now，is_preferred仅缓存 |
| T-502 | [x] | 必须 | 多层/根部永久扫描、格式过滤、完整scan_runs/scan_seen对账；每日全量 |
| T-503 | [x] | 必须 | 按binding+resource转存去重、独占子目录、远端未知reconcile、就绪资产落库 |
| T-504 | [x] | 必须 | Janitor所有权/父链/根重叠校验、播放租约互斥、删除提交/确认及隔离状态 |
| T-505 | [x] | 必须 | Resolver显式版本不换版；默认Tier分组/有界回退；只对确认not_found标missing |
| T-506 | [x] | 必须 | 预准备接口服务、8秒等待与503/Retry-After/稳定错误；移除占位媒体假设 |
| T-507 | [x] | 必须 | 播放租约与资产generation；删除影片软删除/在途产物接管/独立清理 |
| T-508 | [x] | 必须 | 集成故障注入：提交后崩溃、超时、过期、删除未知、根移动/重叠、版本并发 |

## 验收与交付

1. 同版本只一活动任务，不同版本独立；未知提交先对账，不盲发。
2. 新会话TTL过期不可播，旧有效租约能继续；关闭Janitor不改变新会话判定。
3. 永久库消失可转其他类型；无候选明确失败，不循环重扫。
4. 活跃播放、外部移动、根冲突、删除重启与影片删除均不误删/遗忘资产。
5. 冷准备不写进度，不返回占位短片；真正就绪才302。

交付实现、对应契约/故障样例、测试结果及阶段状态。构建和测试要求见总计划；涉及后续阶段的端到端行为由P8统一验收，不以早期接口桩替代正式结果。
