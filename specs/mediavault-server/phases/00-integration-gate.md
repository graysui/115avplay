# G0 · 外部契约与客户端验证门槛

状态：未开始。G0 是能力验证门槛，不是原阶段 P0 的别名。上级见 [plan.md](../plan.md)，证据格式见 [外部集成合同](../../../references/external_integration_contract.md)。

## 前置条件

明确的测试 115 应用/账号、授权方式、归属独立的测试目录、可使用的 btih 与 ed2k 资源、Infuse/VidHub 设备与版本。凭据通过本机加密配置使用，不写入 fixture 或 Git。凭据和客户端未就绪时记录阻塞项，不把网络 mock 通过当作真实验证通过。

## 任务

| ID | 状态 | 优先级 | 工作与产物 |
|---|---|---|---|
| T-G01 | [ ] | 必须 | 验证初次授权和刷新，记录应用限制/scope、身份、轮换语义及脱敏 fixture |
| T-G02 | [ ] | 必须 | 完成 E115-2～4：列目录、btih/ed2k 两条转存链路、远端任务定位、提交未知恢复 |
| T-G03 | [ ] | 必须 | E115-5～7：CDN Range/UA/IP/代理、删除完成/额度、错误分类 |
| T-G04 | [ ] | 必须 | EJ-1/2：JavDB 详情字段与分页契约；固化签名/参数适配依据 |
| T-G05 | [ ] | 必须 | EC-1/2：用最小协议探针验证两客户端登录、协商、选版、冷准备/手动重试/续播 |
| T-G06 | [ ] | 必须 | 记录各能力 passed/failed/blocked、环境版本、限制和 fixture 校验值；据此冻结 P2/P4/P6 的实际外部请求契约 |

## 验收

E115-1～7、EJ-1/2、EC-1/2 各有真实结果。支持范围内失败项有修复后的重验记录；受限模式明确不承诺兼容。G0 不要求完成正式 Go 工程，可以用隔离探针和现有参考进行验证，但不能将探针缺失功能留给客户端自行猜测。

P0/P1 本地工程和迁移可先行；P2/P4/P6 各自依赖的 G0 子门槛未通过时，不允许将其集成验收标完成。工期暂作 3～7 人日探索预算，外部账号/设备等待不计入，结果可能导致重新估算。
